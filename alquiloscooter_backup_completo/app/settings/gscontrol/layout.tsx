
export const metadata = {
  title: 'Integración GSControl',
  description: 'Configuración y sincronización con ALQUILOSCOOTER API',
};

export default function GSControlLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
